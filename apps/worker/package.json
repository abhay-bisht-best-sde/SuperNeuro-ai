{
  "name": "@repo/python-worker",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn worker.main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uv run uvicorn worker.main:app --host 0.0.0.0 --port 8000"
  }
}
